services:
  - type: web
    name: sentinel-web
    runtime: docker
    dockerfilePath: ./infrastructure/docker/web.Dockerfile
    dockerContext: .
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://sentinel-api.onrender.com
      - key: NEXT_PUBLIC_WS_URL
        value: wss://sentinel-api.onrender.com
    healthCheckPath: /
    autoDeploy: yes

  - type: web
    name: sentinel-api
    runtime: docker
    dockerfilePath: ./infrastructure/docker/api.Dockerfile
    dockerContext: .
    envVars:
      - key: REDIS_HOST
        fromService:
          type: redis
          name: sentinel-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: redis
          name: sentinel-redis
          property: port
      - key: EMBEDDED_WORKER
        value: "true"
      - key: SIMULATION_MODE
        value: "true"
      - key: NODE_ENV
        value: production
    healthCheckPath: /public
    autoDeploy: yes

  - type: redis
    name: sentinel-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []
